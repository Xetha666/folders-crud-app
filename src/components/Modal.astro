---
interface Props {
  id: string;
  formClass?: string;
}
const { id, formClass } = Astro.props;
---

<dialog id={id} class="modal modal-bottom sm:modal-middle backdrop-blur-lg">
  <div class="modal-box bg-tactical-dark border border-tactical-olive/50 shadow-2xl">
    <form method="dialog" class={`${formClass}`}>
      <button class="btn btn-sm btn-circle btn-ghost fixed right-2 top-2 text-gray-400 hover:text-white">✕</button>
    </form>
    <div >
      <slot />
    </div>
  </div>
</dialog>